meta:
  environment: python
  toolchain:
    name: pip
    version: "3.8"
build:
  requirementsPath: requirements.txt
run:
  scriptName: aosnova.py
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
